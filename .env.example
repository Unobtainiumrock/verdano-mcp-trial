# Copy this file to `.env` and populate the values below.
# `.env` is gitignored; never commit credentials.

# Trial ERP base URL (per the README brief).
VERDANO_ERP_BASE_URL=https://erp.corvera.ai

# Trial API key — copy from the README brief in this repo.
VERDANO_ERP_API_KEY=

# Optional. Absolute path to the repo root — set automatically by setup-mcp.sh.
# Needed when the MCP host launches the server from a different working directory.
# VERDANO_PROJECT_ROOT=/absolute/path/to/challenge

# Optional. Path to a DuckDB file for local persistence (not used in trial scope
# but plumbed through src/verdano/config.py per D-002). Default: .local/verdano.duckdb
# VERDANO_DUCKDB_PATH=.local/verdano.duckdb

# --- Pipeline thresholds (all have sensible defaults; override to tune) ---
# VERDANO_TAU_SAFE=0.90
# VERDANO_AUTO_THRESHOLD=0.90
# VERDANO_TFIDF_MIN_SCORE=0.5
# VERDANO_TFIDF_MIN_MATCHED_TOKENS=2
# VERDANO_FUZZY_JW_MIN_SCORE=0.30
# Legacy alias: VERDANO_E4_MIN_SCORE also accepted for backward compatibility
# VERDANO_FS_EPSILON=0.02
# VERDANO_FS_GAMMA=0.10
# VERDANO_FS_ALPHA=1.5
# VERDANO_DRIFT_THRESHOLD_LOW=0.5
# VERDANO_DRIFT_THRESHOLD_HIGH=1.5
# VERDANO_DRIFT_RESIDUAL_THRESHOLD=0.10
# VERDANO_DEPOT_FUZZY_THRESHOLD=75
# VERDANO_DEPOT_LLM_MIN_CONFIDENCE=0.50

# --- LLM integration (optional) ---
# Provider-agnostic via OpenAI-compatible SDK. Set base_url for local models.
# VERDANO_LLM_MODEL=gpt-4o
# VERDANO_LLM_API_KEY=
# VERDANO_LLM_BASE_URL=https://api.openai.com/v1

# --- LLM re-ranker (D-019) ---
# Post-cascade validator for low-confidence or ambiguous mappings.
# VERDANO_RERANK_THRESHOLD=0.92
# VERDANO_RERANK_STRATA=tfidf_overlap,fuzzy_jw
# VERDANO_RERANK_MIN_LLM_CONFIDENCE=0.70

# --- Optional, for environments behind a TLS-MITM proxy ---
# The ERP client auto-detects these standard env vars and combines their CA
# bundles with certifi's defaults. Leave unset in normal environments.
# REQUESTS_CA_BUNDLE=/path/to/your-corp-ca.pem
# NODE_EXTRA_CA_CERTS=/path/to/your-corp-ca.pem
# SSL_CERT_FILE=/path/to/explicit-bundle.pem  # explicit override
